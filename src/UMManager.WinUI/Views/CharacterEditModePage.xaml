<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="UMManager.WinUI.Views.CharacterEditModePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="CharacterEditModePageRoot"
    mc:Ignorable="d">

    <Grid Margin="0,-24,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                HorizontalAlignment="Left"
                FontSize="28"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="角色编辑模式" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Click="AddCharacterButton_OnClick" Content="添加角色" />
                <Button Command="{Binding RefreshCommand}" Content="刷新" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Padding="0,0,10,0" RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <AutoSuggestBox
                    PlaceholderText="搜索角色..."
                    QueryIcon="Find"
                    Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <ListView
                    x:Name="CharacterList"
                    Grid.Row="1"
                    ItemsSource="{Binding FilteredCharacters}"
                    SelectedItem="{Binding SelectedCharacter, Mode=TwoWay}"
                    SelectionMode="Single"
                    SelectionChanged="CharacterList_OnSelectionChanged">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel CacheLength="4" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,4" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="FocusVisualPrimaryBrush" Value="Transparent" />
                            <Setter Property="FocusVisualSecondaryBrush" Value="Transparent" />
                            <Setter Property="FocusVisualPrimaryThickness" Value="0" />
                            <Setter Property="FocusVisualSecondaryThickness" Value="0" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Grid>
                                            <Border
                                                x:Name="RootBorder"
                                                Padding="10"
                                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                                BorderThickness="1"
                                                CornerRadius="12">
                                                <ContentPresenter
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    ContentTransitions="{TemplateBinding ContentTransitions}"
                                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                            </Border>

                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal" />
                                                    <VisualState x:Name="PointerOver">
                                                        <VisualState.Setters>
                                                            <Setter Target="RootBorder.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="Pressed">
                                                        <VisualState.Setters>
                                                            <Setter Target="RootBorder.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="Disabled">
                                                        <VisualState.Setters>
                                                            <Setter Target="RootBorder.Opacity" Value="0.55" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                                <VisualStateGroup x:Name="CharacterStates">
                                                    <VisualState x:Name="EnabledCharacter" />
                                                    <VisualState x:Name="DisabledCharacter">
                                                        <VisualState.StateTriggers>
                                                            <StateTrigger IsActive="{Binding Content.IsDisabled, RelativeSource={RelativeSource TemplatedParent}}" />
                                                        </VisualState.StateTriggers>
                                                        <VisualState.Setters>
                                                            <Setter Target="RootBorder.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="56" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border
                                    Width="52"
                                    Height="52"
                                    CornerRadius="10"
                                    Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                    <Image
                                        Width="52"
                                        Height="52"
                                        Stretch="UniformToFill"
                                        Source="{Binding ImageUri}" />
                                </Border>

                                <StackPanel Grid.Column="1" Spacing="2">
                                    <Grid ColumnSpacing="4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            VerticalAlignment="Bottom"
                                            Style="{ThemeResource BodyStrongTextBlockStyle}"
                                            Text="{Binding DisplayName}" />
                                        <TextBlock
                                            Grid.Column="1"
                                            VerticalAlignment="Bottom"
                                            Opacity="0.65"
                                            Style="{ThemeResource CaptionTextBlockStyle}"
                                            Text="{Binding InternalName.Id}" />
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <TextBlock Text="{Binding ModsCountText}" />
                                        <TextBlock Text="{Binding EnabledModsCountText}" />
                                    </StackPanel>
                                </StackPanel>
                                <TextBlock
                                    Grid.Column="2"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                    Text="已禁用"
                                    ToolTipService.ToolTip="该角色已禁用"
                                    Visibility="{Binding IsDisabled, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Frame x:Name="EditFrame" Grid.Column="1" />
        </Grid>
    </Grid>
</Page>
